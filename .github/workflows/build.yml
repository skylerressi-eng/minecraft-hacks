name: Build Phantom Client JAR

on:
  push:
    branches: [ 'claude/minecraft-hack-client-IPi0a', 'main' ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3

      - name: Build mod JAR
        run: |
          chmod +x gradlew
          ./gradlew jar --no-daemon

      - name: Upload mod JAR
        uses: actions/upload-artifact@v4
        with:
          name: phantom-client-mod
          path: build/libs/phantom-client-*.jar
          if-no-files-found: error

      - name: Create Release
        if: github.ref == 'refs/heads/claude/minecraft-hack-client-IPi0a'
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v3.0.0-${{ github.run_number }}
          name: "Phantom Client v3.0.0 (Build #${{ github.run_number }})"
          body: |
            ## Phantom Client v3.0.0

            Drop the `.jar` file into your `.minecraft/mods/` folder.
            Requires: Minecraft 1.21.1 + Fabric Loader 0.16.0+ + Fabric API

            **Controls:**
            - Right Shift: Open/close GUI
            - 1-7: Switch game modes
            - V: Toggle AutoBreachSwap
            - Click modules in the GUI to enable/disable

            **58 modules across 7 game modes:**
            - All Hacks (see all modules)
            - Mace PvP (8 modules)
            - 1.8 PvP (7 modules)
            - Bedwars (6 modules)
            - Crystal PvP (7 modules)
            - Meteor (20 modules)
            - Vape (10 modules)

            **Stealth Engine:**
            - Staff detection & auto-throttle
            - Suspicion scoring with panic mode
            - Anti-cheat profiling (Watchdog, Grim, Vulcan, etc.)
            - Humanized timing on all actions
          files: build/libs/phantom-client-*.jar
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
