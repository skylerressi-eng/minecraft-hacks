# Fabric Properties — Minecraft 1.21.1
# Real, resolvable coordinates from the official fabric-example-mod 1.21.1 branch.
# (The previous 1.21.11 / loom 1.16 / loader 0.19.2 values do not exist on the
# maven repos, so Loom failed at dependency resolution and never built a jar.)
minecraft_version=1.21.1
loader_version=0.16.5
loom_version=1.7-SNAPSHOT
fabric_version=0.102.0+1.21.1

# Mod Properties
mod_version=3.0.0
maven_group=com.github.hackclient
archives_base_name=phantom-client

# Gradle
org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true
